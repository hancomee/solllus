package com.solllus.web.controller.domain;

import java.nio.file.Path;

public class ContentMap_apps extends ContentMap {

    // root : "users"
    public ContentMap_apps(Path rootPath, String root, String name) {
        super(rootPath, root, name);
    }

}
